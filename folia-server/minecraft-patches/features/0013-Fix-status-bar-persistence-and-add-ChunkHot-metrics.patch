From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Chris <90093334+Chwitst187@users.noreply.github.com>
Date: Wed, 19 Aug 2026 21:13:34 +0200
Subject: [PATCH] Fix status bar persistence and add ChunkHot metrics


diff --git a/ca/spottedleaf/moonrise/paper/util/BaseChunkSystemHooks.java b/ca/spottedleaf/moonrise/paper/util/BaseChunkSystemHooks.java
index 52dcbcf64c3a510d4a4524b7d0ee226d9a73511d..baf6caf19ede18729de2b66eaead00ac7203b32c 100644
--- a/ca/spottedleaf/moonrise/paper/util/BaseChunkSystemHooks.java
+++ b/ca/spottedleaf/moonrise/paper/util/BaseChunkSystemHooks.java
@@ -122,6 +122,7 @@ public abstract class BaseChunkSystemHooks implements ca.spottedleaf.moonrise.co
 
     @Override
     public void onChunkNotTicking(final LevelChunk chunk, final ChunkHolder holder) {
+        chunk.folia$getChunkHot().clear(); // Folia - status bars
         chunk.getLevel().getCurrentWorldData().removeTickingChunk(chunk); // Folia - region threading
     }
 
diff --git a/net/minecraft/server/MinecraftServer.java b/net/minecraft/server/MinecraftServer.java
index be15a671e0b39b455cd5983f4b90f8f3022a2ebc..4dbddb7fbd9019c675b27d544daba3292981584c 100644
--- a/net/minecraft/server/MinecraftServer.java
+++ b/net/minecraft/server/MinecraftServer.java
@@ -1739,7 +1739,16 @@ public abstract class MinecraftServer extends ReentrantBlockableEventLoop<TickTa
         // Folia end - region threading
         //this.tickCount++; // Folia - region threading
         //this.tickRateManager.tick(); // Folia - region threading
+        final java.util.ArrayList<net.minecraft.world.level.chunk.LevelChunk> folia$sampledChunks = new java.util.ArrayList<>(); // Folia - status bars
+        if (region != null && io.papermc.paper.threadedregions.RegionizedServer.getCurrentTick() % 20 == 0) { // Folia - status bars
+            for (final net.minecraft.world.level.chunk.LevelChunk chunk : region.world.getCurrentWorldData().getTickingChunks()) {
+                if (!ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(region.world, chunk.locX, chunk.locZ)) continue;
+                folia$sampledChunks.add(chunk);
+                chunk.folia$getChunkHot().nextWindow();
+            }
+        }
         this.tickChildren(haveTime, region); // Folia - region threading
+        for (final net.minecraft.world.level.chunk.LevelChunk chunk : folia$sampledChunks) chunk.folia$getChunkHot().finishWindow(); // Folia - status bars
         if (false && nano - this.lastServerStatus >= STATUS_EXPIRE_TIME_NANOS) { // Folia - region threading
             this.lastServerStatus = nano;
             this.status = this.buildServerStatus();
diff --git a/net/minecraft/server/level/ServerLevel.java b/net/minecraft/server/level/ServerLevel.java
index 35ab432bbd59aba80a6eaca1769e6aa82c8a7e46..338cd27aef69ed4b36c53e92787d0bac66aea33b 100644
--- a/net/minecraft/server/level/ServerLevel.java
+++ b/net/minecraft/server/level/ServerLevel.java
@@ -1488,6 +1488,8 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         final boolean isActive = io.papermc.paper.entity.activation.ActivationRange.checkIfActive(entity); // Paper - EAR 2
         // Folia start - profiler
         final int timerId = isActive ? entity.getType().tickTimerId : entity.getType().inactiveTickTimerId;
+        final LevelChunk folia$chunk = entity instanceof Player ? null : this.getChunkIfLoaded(entity.moonrise$getSectionX(), entity.moonrise$getSectionZ()); // Folia - status bars
+        if (folia$chunk != null) folia$chunk.folia$getChunkHot().startTicking(); // Folia - status bars
         final ca.spottedleaf.leafprofiler.RegionizedProfiler.Handle foliaProfiler = io.papermc.paper.threadedregions.TickRegionScheduler.getProfiler();
         foliaProfiler.startTimer(timerId);
         try {
@@ -1506,7 +1508,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         // Folia end - region threading
         } else {entity.inactiveTick();} // Paper - EAR 2
         profiler.pop();
-        } finally { foliaProfiler.stopTimer(timerId); } // Folia - profiler
+        } finally { foliaProfiler.stopTimer(timerId); if (folia$chunk != null) folia$chunk.folia$getChunkHot().stopTickingAndCount(); } // Folia - profiler // Folia - status bars
 
         for (Entity passenger : entity.getPassengers()) {
             this.tickPassenger(entity, passenger, isActive); // Paper - EAR 2
@@ -1524,6 +1526,8 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
         } else if (entity instanceof Player || this.getCurrentWorldData().hasEntityTickingEntity(entity)) { // Folia - region threading
             // Folia start - profiler
             final int timerId = isActive ? entity.getType().tickTimerId : entity.getType().inactiveTickTimerId;
+            final LevelChunk folia$chunk = entity instanceof Player ? null : this.getChunkIfLoaded(entity.moonrise$getSectionX(), entity.moonrise$getSectionZ()); // Folia - status bars
+            if (folia$chunk != null) folia$chunk.folia$getChunkHot().startTicking(); // Folia - status bars
             final ca.spottedleaf.leafprofiler.RegionizedProfiler.Handle foliaProfiler = io.papermc.paper.threadedregions.TickRegionScheduler.getProfiler();
             foliaProfiler.startTimer(timerId);
             try {
@@ -1559,7 +1563,7 @@ public class ServerLevel extends Level implements WorldGenLevel, ServerEntityGet
             for (Entity passenger : entity.getPassengers()) {
                 this.tickPassenger(entity, passenger, isActive); // Paper - EAR 2
             }
-            } finally { foliaProfiler.stopTimer(timerId); } // Folia - profiler
+            } finally { foliaProfiler.stopTimer(timerId); if (folia$chunk != null) folia$chunk.folia$getChunkHot().stopTickingAndCount(); } // Folia - profiler // Folia - status bars
         }
     }
 
diff --git a/net/minecraft/server/level/ServerPlayer.java b/net/minecraft/server/level/ServerPlayer.java
index 16f49ae6736c475207f31392307e280ec06edbc3..979f7264b7cf47b3974a7c999ae8f5b89c4d0b65 100644
--- a/net/minecraft/server/level/ServerPlayer.java
+++ b/net/minecraft/server/level/ServerPlayer.java
@@ -230,6 +230,11 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
     );
     private static final boolean DEFAULT_SEEN_CREDITS = false;
     private static final boolean DEFAULT_SPAWN_EXTRA_PARTICLES_ON_FALL = false;
+    public boolean tpsBarEnabled = false; // Folia - status bars
+    public boolean memBarEnabled = false; // Folia - status bars
+    public boolean regionBarEnabled = false; // Folia - status bars
+    public boolean cpuBarEnabled = false; // Folia - status bars
+    private volatile long folia$nearbyChunkHot; // Folia - status bars
     public ServerGamePacketListenerImpl connection;
     private final MinecraftServer server;
     public final ServerPlayerGameMode gameMode;
@@ -659,6 +664,10 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         this.respawnConfig = input.read("respawn", ServerPlayer.RespawnConfig.CODEC).orElse(null);
         this.spawnExtraParticlesOnFall = input.getBooleanOr("spawn_extra_particles_on_fall", false);
         this.raidOmenPosition = input.read("raid_omen_position", BlockPos.CODEC).orElse(null);
+        this.tpsBarEnabled = input.getBooleanOr("tpsbarEnabled", false); // Folia - status bars
+        this.memBarEnabled = input.getBooleanOr("membarEnabled", false); // Folia - status bars
+        this.regionBarEnabled = input.getBooleanOr("regionbarEnabled", false); // Folia - status bars
+        this.cpuBarEnabled = input.getBooleanOr("cpubarEnabled", false); // Folia - status bars
         this.loadGameTypes(input); // Paper - Expand PlayerGameModeChangeEvent
         this.setShoulderEntityLeft(input.read("ShoulderEntityLeft", CompoundTag.CODEC).orElseGet(CompoundTag::new));
         this.setShoulderEntityRight(input.read("ShoulderEntityRight", CompoundTag.CODEC).orElseGet(CompoundTag::new));
@@ -693,6 +702,10 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         output.storeNullable("respawn", ServerPlayer.RespawnConfig.CODEC, this.respawnConfig);
         output.putBoolean("spawn_extra_particles_on_fall", this.spawnExtraParticlesOnFall);
         output.storeNullable("raid_omen_position", BlockPos.CODEC, this.raidOmenPosition);
+        output.putBoolean("tpsbarEnabled", this.tpsBarEnabled); // Folia - status bars
+        output.putBoolean("membarEnabled", this.memBarEnabled); // Folia - status bars
+        output.putBoolean("regionbarEnabled", this.regionBarEnabled); // Folia - status bars
+        output.putBoolean("cpubarEnabled", this.cpuBarEnabled); // Folia - status bars
         this.saveEnderPearls(output);
         if (!this.getShoulderEntityLeft().isEmpty()) {
             output.store("ShoulderEntityLeft", CompoundTag.CODEC, this.getShoulderEntityLeft());
@@ -937,6 +950,21 @@ public class ServerPlayer extends Player implements ca.spottedleaf.moonrise.patc
         this.trackEnteredOrExitedLavaOnVehicle();
         this.updatePlayerAttributes();
         this.advancements.flushDirty(this, true);
+        if (this.tickCount % 20 == 0) this.folia$nearbyChunkHot = this.folia$refreshNearbyChunkHot(); // Folia - status bars
+    }
+
+    public long folia$getNearbyChunkHot() { return this.folia$nearbyChunkHot; } // Folia - status bars
+
+    private long folia$refreshNearbyChunkHot() { // Folia - status bars
+        long total = 0L;
+        final int radius = ((ServerLevel)this.level()).moonrise$getViewDistanceHolder().getViewDistances().tickViewDistance();
+        for (int chunkX = this.moonrise$getSectionX() - radius; chunkX <= this.moonrise$getSectionX() + radius; ++chunkX) {
+            for (int chunkZ = this.moonrise$getSectionZ() - radius; chunkZ <= this.moonrise$getSectionZ() + radius; ++chunkZ) {
+                final net.minecraft.world.level.chunk.LevelChunk chunk = this.level().getChunkIfLoaded(chunkX, chunkZ);
+                if (chunk != null) total += chunk.folia$getChunkHot().average();
+            }
+        }
+        return total;
     }
 
     private void updatePlayerAttributes() {
diff --git a/net/minecraft/world/level/chunk/LevelChunk.java b/net/minecraft/world/level/chunk/LevelChunk.java
index 64e0c4a60a74cd33f2b99f34fa2a419abf3a0c02..6ee95dfba21bc7fc66e7fe03561812c0e01e5478 100644
--- a/net/minecraft/world/level/chunk/LevelChunk.java
+++ b/net/minecraft/world/level/chunk/LevelChunk.java
@@ -112,6 +112,8 @@ public class LevelChunk extends ChunkAccess implements DebugValueSource, ca.spot
     // Paper start - rewrite chunk system
     private boolean postProcessingDone;
     private ca.spottedleaf.moonrise.patches.chunk_system.scheduling.NewChunkHolder chunkAndHolder;
+    private final io.papermc.paper.threadedregions.ChunkHot folia$chunkHot = new io.papermc.paper.threadedregions.ChunkHot(); // Folia - status bars
+    public io.papermc.paper.threadedregions.ChunkHot folia$getChunkHot() { return this.folia$chunkHot; } // Folia - status bars
 
     @Override
     public final boolean moonrise$isPostProcessingDone() {
@@ -959,6 +961,7 @@ public class LevelChunk extends ChunkAccess implements DebugValueSource, ca.spot
                     try {
                         ProfilerFiller profiler = Profiler.get();
                         profiler.push(this::getType);
+                        LevelChunk.this.folia$chunkHot.startTicking(); // Folia - status bars
                         foliaProfiler.startTimer(timerId); try { // Folia - profiler
                         BlockState blockState = LevelChunk.this.getBlockState(pos);
                         if (this.blockEntity.getType().isValid(blockState)) {
@@ -979,7 +982,7 @@ public class LevelChunk extends ChunkAccess implements DebugValueSource, ca.spot
                                 );
                             } // Paper - Remove the Block Entity if it's invalid
                         }
-                        } finally { foliaProfiler.stopTimer(timerId); } // Folia - profiler
+                        } finally { foliaProfiler.stopTimer(timerId); LevelChunk.this.folia$chunkHot.stopTickingAndCount(); } // Folia - profiler // Folia - status bars
 
                         profiler.pop();
                     } catch (Throwable t) {
diff --git a/net/minecraft/world/level/redstone/NeighborUpdater.java b/net/minecraft/world/level/redstone/NeighborUpdater.java
index 041ed95948ff6bb4cf3610ce4522a6ba105b6812..1076885449c24bb0b7dc7a91392fc227241c4ab7 100644
--- a/net/minecraft/world/level/redstone/NeighborUpdater.java
+++ b/net/minecraft/world/level/redstone/NeighborUpdater.java
@@ -81,7 +81,13 @@ public interface NeighborUpdater {
                 return;
             }
             // CraftBukkit end
-            state.handleNeighborChanged(level, pos, changedBlock, orientation, movedByPiston);
+            final net.minecraft.world.level.chunk.LevelChunk folia$chunk = level.getChunkIfLoaded(pos); // Folia - status bars
+            if (folia$chunk != null) folia$chunk.folia$getChunkHot().startTicking(); // Folia - status bars
+            try {
+                state.handleNeighborChanged(level, pos, changedBlock, orientation, movedByPiston);
+            } finally {
+                if (folia$chunk != null) folia$chunk.folia$getChunkHot().stopTickingAndCount(); // Folia - status bars
+            }
             // Spigot start
         } catch (StackOverflowError ex) {
             level.lastPhysicsProblem = pos.immutable();
